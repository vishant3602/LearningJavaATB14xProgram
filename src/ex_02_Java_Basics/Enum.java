package ex_02_Java_Basics;

public class Enum {

    // enum is a keyword and we can not use it as identifier.
    // But Java is case-sensitive and we have used Enum (E is capital),
    // So, it is different word and we can use it as identifier.
}
